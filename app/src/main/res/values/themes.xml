<resources>
    <!--
      Minimal window theme only. All styling lives in Compose (ui/theme).
      Light-only by design: the design is light, and a second token set would
      double both the theme work and the screenshot checks.
    -->
    <style name="Theme.Everfit" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
